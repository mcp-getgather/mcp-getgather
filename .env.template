# Application Settings
ENVIRONMENT=local
APP_NAME=getgather-local
LOG_LEVEL=DEBUG

# Logging
SENTRY_DSN=

OPENAI_API_KEY=
BROWSER_USE_MODEL=

# Proxy Configuration System
# Configure multiple proxies using indexed environment variables.
# Format: PROXY_{INDEX}_{COMPONENT}
#
# Default proxy type (optional - will be used if no x-proxy-type header is provided)
# DEFAULT_PROXY_TYPE=proxy-1
DEFAULT_PROXY_TYPE=

# proxy-0: No proxy
PROXY_0_TYPE=none

# proxy-1: Example Proxy Config
# PROXY_1_TYPE=proxy_1_type
# PROXY_1_URL=http://proxy-1.io:8889
# PROXY_1_BASE_USERNAME=customer-mcpgg
# PROXY_1_PARAMS_TEMPLATE=sessid-{session_id}-country_{country}_city_{city}_state_{state}_postalcode_{postal_code}
# PROXY_1_PASSWORD=your_password_here

# proxy-2: Example Proxy 2 Config
# PROXY_2_TYPE=proxy_2_type
# PROXY_2_URL=http://gate.proxy-2.com:7000
# PROXY_2_BASE_USERNAME=user-mcpgg
# PROXY_2_PARAMS_TEMPLATE=session-{session_id}-country-{country}-state-us_{state}-city-{city}
# PROXY_2_PASSWORD=your_password

# Template placeholders (only segments with values will be included):
#   {session_id}  - Replaced with profile_id (unique per browser session)
#   {country}     - Country code (e.g., us, id, jp)
#   {state}       - State/province
#   {city}        - City name
#   {postal_code} - Postal/zip code
#
# To use: Set x-proxy-type header to "proxy-0", "proxy-1", etc.

HOSTNAME=

# where persisted data is stored
DATA_DIR=
AUTH_BEARER_TOKEN=

# Application Settings
ENVIRONMENT=local
APP_NAME=getgather-local
LOG_LEVEL=DEBUG

# Logging
SENTRY_DSN=

OPENAI_API_KEY=
BROWSER_USE_MODEL=

# Proxy Configuration System
# Configure multiple proxies using indexed environment variables.
# Credentials are embedded in the URL: username:password@host:port
#
# Default proxy (optional - will be used if no x-proxy-type header is provided)
# Set to "proxy-1", "proxy-2", etc.
# DEFAULT_PROXY_TYPE=proxy-1
DEFAULT_PROXY_TYPE=

# proxy-1: Example proxy configuration
# PROXY_1_URL=http://user-mcpgg:your_password@proxy-1.io:8889
# PROXY_1_PARAMS_TEMPLATE=sessid-{session_id}-country_{country}_city_{city}_state_{state}

# proxy-2: Another example proxy configuration
# PROXY_2_URL=http://user-mcpgg:your_password@proxy-2.io:7000
# PROXY_2_PARAMS_TEMPLATE=session-{session_id}-cc-{country}-state-us_{state}-city-{city}

# Template placeholders (only segments with values will be included):
#   {session_id}  - Replaced with profile_id (unique per browser session)
#   {country}     - Country code (e.g., us, id, jp)
#   {state}       - State/province
#   {city}        - City name
#   {postal_code} - Postal/zip code

HOSTNAME=

# where persisted data is stored
DATA_DIR=
AUTH_BEARER_TOKEN=
